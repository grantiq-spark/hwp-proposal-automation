{
  "name": "hwp-conversion-saas",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP wrapper for DOCX to HWPX conversion and visual comparison using Word, Hangul, ImageMagick, and Ghostscript.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
